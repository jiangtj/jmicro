# TODO

- 补充 `FormRuleGenerator` 对更多 `jakarta.validation` 注解的规则映射（如 `@Positive`、`@DecimalMin`、`@Past` 等）。
- 增加 `JsonUtils` 的常见配置示例（如时间序列化策略、空值处理策略）。
- 提供 `MicroProblemDetailException` 与 Spring `ProblemDetail` 对接的示例。
